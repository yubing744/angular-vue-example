export const getHeaderTitle = state => {
    return state.headerTitle;
}

export const getNavItems = state => {
    return state.sideBarItems;
}

export const getMainView = state => {
    return state.mainView;
}